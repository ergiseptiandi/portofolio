version: '3'
services:
  portfolio:
    build: .
    ports:
      # - "3000:3000"
    env_file:
      - /home/runner/portfolio.env
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: 100M
